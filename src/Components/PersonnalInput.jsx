import {connect, getIn} from "formik";
import { Checkbox, Input } from "@chakra-ui/react";

/*export const PersonnalCheckBox = ( props ) => {
  return(
    <>
      <Checkbox  
          name="adressedefaut"
          id="adressedefaut"
          
          onChange={props.formik.handleChange}
          onBlur={props.formik.handleBlur}
          value={getIn(props.formik.values, "adressedefaut")}
      >
          Par défaut
      </Checkbox>
      <div>
        {props.formik.values.quantite}
      </div>
    </>  
  )
}*/
//export default connect(PersonnalCheckBox)

